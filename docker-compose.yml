version: "3.8"

services:
  database:
    image: postgres
    ports:
      - "5432"
    environment:
      POSTGRES_USER: test
      POSTGRES_DB: test
      POSTGRES_PASSWORD: test
    networks:
      - bridge

  facebook_replica:
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - database
    networks:
      - bridge

networks:
  bridge:
    driver: bridge
